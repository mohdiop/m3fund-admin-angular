import { ProjectDomainPipe } from './project-domain-pipe';

describe('ProjectDomainPipe', () => {
  it('create an instance', () => {
    const pipe = new ProjectDomainPipe();
    expect(pipe).toBeTruthy();
  });
});
