import { NumberSpacesPipePipe } from './number-spaces-pipe-pipe';

describe('NumberSpacesPipePipe', () => {
  it('create an instance', () => {
    const pipe = new NumberSpacesPipePipe();
    expect(pipe).toBeTruthy();
  });
});
