import { OwnerTypePipe } from './owner-type-pipe';

describe('OwnerTypePipe', () => {
  it('create an instance', () => {
    const pipe = new OwnerTypePipe();
    expect(pipe).toBeTruthy();
  });
});
