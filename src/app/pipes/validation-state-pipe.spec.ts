import { ValidationStatePipe } from './validation-state-pipe';

describe('ValidationStatePipe', () => {
  it('create an instance', () => {
    const pipe = new ValidationStatePipe();
    expect(pipe).toBeTruthy();
  });
});
